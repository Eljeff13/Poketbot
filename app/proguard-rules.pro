# The game keeps all of its state in memory and in SharedPreferences primitives,
# so no reflection-based rules are needed. Keep line numbers for readable
# Play Console crash reports.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
